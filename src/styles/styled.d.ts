declare module 'styled-components/native' {
  export interface DefaultTheme {
    name: string;
    colors: {
      background: string;
      buttons: string;
      text: string;
      title: string;
    };
  }
  export interface SecondTheme {
    name: string;
    colors: {
      background: string;
      buttons: string;
      text: string;
      title: string;
    };
  }
}
