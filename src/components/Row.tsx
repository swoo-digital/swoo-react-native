import styled from 'styled-components/native'
import { color, flexbox, flex, layout } from 'styled-system'

const Row = styled.View`
  ${color}
  ${flexbox}
  ${flex}
  ${layout}
`

export default Row
